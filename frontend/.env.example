# Frontend Environment Variables

# API Configuration
# By default, the frontend uses the proxy setting in package.json
# Set this to 'false' to disable proxy and use direct API URL
REACT_APP_USE_PROXY=true

# Custom API URL (only used if REACT_APP_USE_PROXY=false)
# REACT_APP_API_URL=http://localhost:5000/api

# Port for the React development server (default: 3000)
PORT=3000

# Disable automatic browser opening
BROWSER=none

# Environment (development, production, test)
NODE_ENV=development